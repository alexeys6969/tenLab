<Page x:Class="tenLabPractice.Pages.mainReport"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:lvc="clr-namespace:LiveChartsCore.SkiaSharpView.WPF;assembly=LiveChartsCore.SkiaSharpView.WPF"
      mc:Ignorable="d" 
      d:DesignHeight="1024" d:DesignWidth="1440"
      Background="#202020"
      Title="mainReport">

    <Grid>
        <StackPanel Margin="100,0,100,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                <Image Source="/Images/tenLabLogo.png" Width="197" Height="57" HorizontalAlignment="Left" Margin="0,60,0,0"/>
                <ScrollViewer Margin="40,63,0,0" HorizontalScrollBarVisibility="Hidden" MaxWidth="925" VerticalScrollBarVisibility="Disabled">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <Button x:Name="Report" Content="Отчет" BorderThickness="0" Background="#333333" Width="183" Height="51" FontSize="20" Style="{StaticResource ButtonStyle}"/>
                        <Button x:Name="Usage" Content="Заявка на использование" BorderThickness="0" Background="#333333" Width="305" Height="51" FontSize="20" Style="{StaticResource ButtonStyle}" Margin="5,0,0,0"/>
                        <Button x:Name="Service" Content="Заявка на обслуживание" BorderThickness="0" Background="#333333" Width="305" Height="51" FontSize="20" Style="{StaticResource ButtonStyle}" Margin="5,0,0,0"/>
                        <Button x:Name="Usable" Content="Используемые устройства" BorderThickness="0" Background="#333333" Width="305" Height="51" FontSize="20" Style="{StaticResource ButtonStyle}" Margin="5,0,0,0"/>
                        <Button x:Name="Servicable" Content="Обслуживаемые устройства" BorderThickness="0" Background="#333333" Width="305" Height="51" FontSize="20" Style="{StaticResource ButtonStyle}" Margin="5,0,0,0"/>
                    </StackPanel>
                </ScrollViewer>
                <Button Width="70" Height="70" Margin="8,54,0,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="#4A7BBA" 
                    CornerRadius="35">
                                <ContentPresenter HorizontalAlignment="Center" 
                                  VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                    <Image Source="/Images/userLogo.png"  Width="60" Height="60"/>
                </Button>
            </StackPanel>
            <Grid Margin="0,100,0,0" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" HorizontalAlignment="Stretch" Margin="0,0,71,0">
                    <TextBlock Text="Всего устройств используется: " x:Name="devUseCount" Style="{StaticResource MainText}" FontSize="32"/>
                    <TextBlock Text="Из них: " x:Name="devUseList" Style="{StaticResource MainText}" Margin="0,20,0,0"/>
                    <Button Content="Подробнее" Style="{StaticResource ButtonStyle}" Background="#333333" BorderThickness="0" Height="51" Margin="0,10,0,0"/>
                </StackPanel>
                <StackPanel HorizontalAlignment="Stretch" Grid.Column="1" Margin="71,0,0,0">
                    <TextBlock Text="Устройств в обслуживании: " x:Name="devServiceCount" Style="{StaticResource MainText}" FontSize="32"/>
                    <TextBlock Text="Из них: " x:Name="devServiceList" Style="{StaticResource MainText}" Margin="0,20,0,0"/>
                    <Button Content="Подробнее" Style="{StaticResource ButtonStyle}" Background="#333333" BorderThickness="0" Height="51" Margin="0,10,0,0"/>
                </StackPanel>
            </Grid>
            <StackPanel Margin="0,12,0,0">
                <TextBlock Text="Последние заявки на обслуживание:" Style="{StaticResource MainText}" FontSize="32"/>
                <ScrollViewer VerticalScrollBarVisibility="Hidden">
                    <StackPanel x:Name="serviceParent"/>
                </ScrollViewer>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
