<UserControl x:Class="tenLabPractice.Items.headerItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:tenLabPractice.Items"
             mc:Ignorable="d">
    <Grid HorizontalAlignment="Stretch" Margin="0,15,0,0">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" Source="/Images/tenLabLogo.png" Width="110" Height="32" VerticalAlignment="Center"/>

        <ScrollViewer Grid.Column="1" Margin="25,0,25,0" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled" VerticalAlignment="Center"
              PreviewMouseDown="ScrollViewer_PreviewMouseDown"
              PreviewMouseMove="ScrollViewer_PreviewMouseMove"
              PreviewMouseUp="ScrollViewer_PreviewMouseUp"
              Cursor="Hand"
              x:Name="menuContainer">
            <StackPanel Orientation="Horizontal">
                <Button x:Name="Report" Click="ReportClick" Content="Отчет" BorderThickness="0" Background="#4A7BBA" Width="100" Height="32" FontSize="12" Style="{StaticResource NavigationButtonStyle}"/>
                <Button x:Name="Usage" Click="UsageClick" Content="Заявка на использование" BorderThickness="0" Background="#333333" Width="170" Height="32" FontSize="12" Style="{StaticResource NavigationButtonStyle}" Margin="8,0,0,0"/>
                <Button x:Name="Service" Click="ServiceClick" Content="Заявка на обслуживание" BorderThickness="0" Background="#333333" Width="170" Height="32" FontSize="12" Style="{StaticResource NavigationButtonStyle}" Margin="8,0,0,0"/>
                <Button x:Name="Usable" Click="UsableClick" Content="Используемые устройства" BorderThickness="0" Background="#333333" Width="180" Height="32" FontSize="12" Style="{StaticResource NavigationButtonStyle}" Margin="8,0,0,0"/>
                <Button x:Name="Servicable" Click="ServicableClick" Content="Обслуживаемые устройства" BorderThickness="0" Background="#333333" Width="190" Height="32" FontSize="12" Style="{StaticResource NavigationButtonStyle}" Margin="8,0,0,0"/>
            </StackPanel>
        </ScrollViewer>

        <Button Grid.Column="2" Width="42" Height="42" HorizontalAlignment="Right" VerticalAlignment="Center">
            <Button.Template>
                <ControlTemplate TargetType="Button">
                    <Border Background="#333333" CornerRadius="21">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Button.Template>
            <Image Source="/Images/userLogo.png" Width="36" Height="36"/>
        </Button>
    </Grid>
</UserControl>
